# Convex Configuration
# After running `npx convex dev`, copy the values shown in the terminal here.
# Rename this file to .env.local (remove the .example part)

CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
